//Write all our pseudo code here

for or while(parse through the lines of the code)
{
	read a line --->
	if(it starts with #)
	{
		skip over and dont write to new file
	}
	if(we come across "=")
	{
		send to function which does variable definitions and copy the new line into our new file
	}
	if(come across target lines)
	{
		send to function which deals with target lines
	}
	if(come across action lines)
	{
		send these modifed action lines to shell 
	}
	
}